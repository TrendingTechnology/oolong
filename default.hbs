<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">

    <title>{{meta_title}}</title>
    <meta name="description" content="{{meta_description}}">

    <meta name="HandheldFriendly" content="True">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="theme-color" content="#ffffff">

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.0.28/css/bulma.min.css">
    <link rel="stylesheet" href="https://use.fontawesome.com/5bbf256271.css">

    {{ghost_head}}
  </head>
  <body>
    <section class="hero is-success">
      <div class="hero-head">
        <header class="nav">
          <div class="container">
            <div class="nav-left">
              {{#if @blog.logo}}
                <a class="nav-item" href=""><img src="{{@blog.logo}}" alt="Logo" width="24px" height="24px"></a>
              {{/if}}
            </div>

            <div class="nav-center">
              {{#if @blog.facebook}}<a class="nav-item" href="{{facebook_url @blog.facebook}}"><span class="icon"><i class="fa fa-facebook"></i></span></a>{{/if}}
              {{#if @blog.twitter}}<a class="nav-item" href="{{twitter_url @blog.twitter}}"><span class="icon"><i class="fa fa-twitter"></i></span></a>{{/if}}
            </div>

            {{navigation}}
          </div>
        </header>
      </div>

      <div class="hero-body">
        <div class="container">
          {{#if @blog.title}}<h1 class="title">{{@blog.title}}</h1>{{/if}}
          {{#if @blog.description}}<p class="subtitle">{{@blog.description}}</p>{{/if}}
        </div>
      </div>
    </section>

    {{{body}}}

    <footer class="footer">
      <div class="container">
        <div class="content has-text-centered">
          <p>All content copyright <strong>{{@blog.title}}</strong> &copy; {{date format="YYYY"}}</p>
          <p>
            <a href="http://bulma.io">Bulma</a> by <a href="http://jgthms.com">Jeremy Thomas</a>.
            Icons by <a href="http://fontawesome.io/">Font Awesome</a>.
            Proudly published with <a href="https://ghost.org">Ghost</a>.
          </p>
        </div>
      </div>
    </footer>

    {{ghost_foot}}
  </body>
</html>
